# ParaGraphiQ
ParaGraphiQ is a graphic EQ VST with some traits lended from the concept of the parametric EQ, like a "Q"-like parameter to smoothen out neighbring bands. It is intended to provide improved usage on mobile phone screens - but here, we have the VST version.
